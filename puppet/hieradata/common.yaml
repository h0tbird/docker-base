---

#------------------------------------------------------------------------------
# r_base:
#------------------------------------------------------------------------------

# ssh
r_base::ssh:
 ensure: 'running'
 enable: true
 version: 'latest'

# terminfo
r_base::terminfo:
 purge: false
 backup: false
 recurse: 'remote'

...
